{
  "name": "blaze-skate-analysis",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/generate-firebase-config.mjs && vite",
    "typecheck": "tsc --noEmit",
    "build": "node scripts/generate-firebase-config.mjs && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "dotenv": "^17.4.2",
    "typescript": "^6.0.3",
    "vite": "^8.0.16"
  },
  "dependencies": {
    "firebase": "^12.15.0"
  }
}
